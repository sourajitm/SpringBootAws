version: 0.2

phases:

  install:
    runtime-versions:
      java: corretto8
      
  pre_build:
    commands:
      - echo test prebuild 1

  build:
    commands:
      - mvn test

  post_build:
    commands:
      - mvn package 
	  - mv target/SpingBootAws-0.0.1-SNAPSHOT.war SpingBootAws-0.0.1-SNAPSHOT.war

artifacts:
  files:
    - target/SpingBootAws-0.0.1-SNAPSHOT.war